/* Read input from STDIN. Print your output to STDOUT*/
#include<stdio.h>
int fact(int n)
{
	if(n==1)
	{
		return 1;
	}
	else if(n==0)
	{
		return 1;
	}
	else
	{
		return n*fact(n-1);
	}
}
int main()
{
	int a,k;
	scanf("%d",&a);
	k=fact(a);
	printf("%d",k);
}
